# Authentication Service

## Password Reset
To initiate a password reset flow, the internal API requires the user's unique `employee_id`. 
This is a security measure to prevent external actors from guessing usernames.